- if @teams.nil? 
    %h4 
- else 
    %h5  #{@teams.length} results found
    %table
        %thead
            %tr 
            %th
            %th
        %tbody
            - @teams.each do |team|
                %tr 
                %td= team.name
                %td= link_to 'Make this my team', user_path()
                 