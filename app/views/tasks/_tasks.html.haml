= link_to 'New task', new_team_task_path(current_team_id), remote: true, :class => :button

-#https://iridakos.com/tutorials/2013/12/07/creating-a-simple-todo-part-1
#modal.modal.fade

%table#tasks
    %thead
        %tr
            %th 
            %th Title
            %th Due
            %th Priority
            %th Description
            %th 
    %tbody
        -@tasks.each do |task|
            - if task.team_id == current_team_id
                %tr{:class => ("complete" if task.complete)}
                    -#:class => task.complete ? 'complete clickable-row' : 'clickable-row', 'data-href' => complete_team_task_path(current_team_id, task)
                    %td= link_to task.complete ? 'Done' : 'Complete', complete_team_task_path(current_team_id, task), :class => 'button link', :method => :complete
                    %td= task.title
                    %td= task.due.strftime("%b %d, %Y")
                    %td= task.priority
                    %td= task.description
                    %td= link_to 'Edit', edit_team_task_path(current_team_id, task), remote: true, :class => "button link"
                    %td= link_to 'Delete', team_task_path(current_team_id, task), :class => "button link", :method => :delete 

-# :javascript
-# //https://stackoverflow.com/questions/6330763/jquery-table-row-click-event-also-firing-when-i-click-a-link
-# //$('.clickable-row .link').click(function(e){
-# //    e.stopPropagation(); 
-# //});
-# $('.clickable-row').hover( function () {
-#     $(this).toggleClass('hover');
-# }).click(function() {
-#     url = $(this).data('href');
-#     console.log(url);
-#     $.ajax({
-#         type: "POST",
-#         url: url
-#     });
-# });
    